Placeholder for simple Drupal module:
- Field `field_temporal_workflow_id`
- Config for REST endpoint
- Node save hook to start workflows based on launch date
- Status block with approve/reschedule buttons
- REST endpoint to publish node when worker calls back
